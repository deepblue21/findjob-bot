# ── GENEL AYARLAR (kişiye özel arama profiles/ klasöründe) ──
telegram:
  bot_token: "${TELEGRAM_BOT_TOKEN}"
  chat_id: "${TELEGRAM_CHAT_ID}"

schedule:
  interval_hours: 3           # her N saatte tüm profiller otomatik taranır
  min_score_to_notify: 3.0    # Telegram'a gönderme eşiği
  min_score_to_store: 1.0     # dashboard'a kaydetme eşiği
  min_relevance: 1.0          # bir ilan en az bu kadar ROL/arama-terimi uyumu içermezse alınmaz

sources:
  kariyer_block_cooldown_hours: 6  # Kariyer 403 verirse tekrar denemeden önce beklenecek süre

# Tüm profillerde ORTAK puanlama (çalışma şekli / seviye).
# Konum önceliği ve kişiye özel beceriler profiles/*.yaml içinden otomatik eklenir.
scoring:
  uzaktan: 2
  remote: 2
  hibrit: 1
  hybrid: 1
  "ar-ge": 1
  stajyer: 1
  intern: 1
  junior: 1
  "entry level": 1
  "mid-level": 1
  senior: 1

# Tüm profillerde ORTAK eleme (her profilin kendi exclude'u buna eklenir)
filters:
  exclude_keywords:
    - "güvenlik görevlisi"
    - "temizlik görevlisi"
    - "engelli"          # engelli-kadro ilanlarını ele
    - "(engelli)"

database:
  path: "~/.job_bot/jobs.db"
